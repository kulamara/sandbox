# 综合安全沙箱需求文档

### 综合安全沙箱项目需求

您是安全开发运维（DevSecOps）和系统架构方面的专家。您的任务是设计并生成一个使用Docker的安全、隔离数据分析沙箱的完整文件集。

该沙箱专为需要处理敏感数据且绝对不能发生数据泄露的分析师而设计。环境必须严格受控、锁定，并且只能通过安全、经过身份验证的方式进行扩展。

**核心设计原则：**

1. **严格数据隔离：** 数据一旦被拉入沙箱，必须通过技术手段防止其通过网络渠道离开容器。
2. **不可变且受控的环境：** 沙箱内的用户不应能够安装新软件、编辑系统文件或创建可执行脚本。环境是预定义和锁定的。
3. **安全可扩展性：** 新的分析工具（脚本）可以添加到沙箱中，但只能通过专用的、经过身份验证的脚本进行。

**详细组件需求：**

基于这些原则，请为以下组件生成完整的源代码：

**1. 自动化部署脚本 (`deploy.sh`)**

* 这应该是一个bash脚本，作为管理员的主要入口点。
* 它应该接受参数来配置沙箱，例如容器名称。
* 它将负责使用`docker-compose`构建和启动Docker环境。

**2. Docker Compose配置 (`docker-compose.yml`)**

* 定义主要的沙箱服务。
* **网络隔离（需求3）：** 创建一个带有`internal: true`标志的自定义Docker网络，以阻止来自外部世界的所有出站和入站流量。容器应该无法`ping google.com`。
* 定义必要的卷：
    * `./data:/data`：用于存储和分析敏感数据。
    * `./command_inbox:/inbox`：一个特定的临时位置，用于从外部接收新脚本。

**3. 强化的Dockerfile (`Dockerfile`)**

* **多阶段构建（需求6）：** 使用多阶段构建过程确保最终镜像最小且安全。
    * **`builder`阶段：** 从完整的操作系统（如`debian`）开始。安装所有必要的构建工具、开发库、Python依赖项（`pip install`），并下载任何所需的客户端（如用于`scp`的`openssh-client`）。
    * **`final`阶段：** 从最小基础镜像（例如`debian:stable-slim`）开始。
        * 创建一个非root用户（例如`appuser`）并切换到该用户。
        * 仅将必要的、预编译的二进制文件和脚本从`builder`阶段复制到最终镜像中（例如复制Python虚拟环境、`scp`二进制文件和我们的自定义脚本）。
        * **关键是，不要在最终阶段安装包管理器（`apt`）、shell（如果需要，`bash`可以替换为`sh`）、编辑器（`vi`、`nano`）或编译器（`gcc`）。**

**4. 核心内部脚本**

```
a. **数据获取脚本 (`fetch_data.sh`)（需求2）**
* 位于`/usr/local/bin/fetch_data`的shell脚本。
* 它应该使用`scp`或`rsync`将数据从远程服务器拉取到`/data`目录。
* 连接详细信息（用户、主机、密钥路径）必须通过环境变量提供，而不是在脚本中硬编码。

b. **敏感数据扫描器 (`sensitive_data_scanner.py`)（需求4）**
* 位于`/usr/local/bin/scan_data`的Python脚本。
* 它应该扫描`/data`目录中的PII（中国手机号码、中国身份证号码、电子邮件）。
* **需求：** 脚本的代码注释必须使用**中文**。

c. **经过身份验证的命令安装器 (`install_command.sh`)（需求5）**
* 位于`/usr/local/bin/install_command`的shell脚本。
* **功能：** 它从`/inbox`卷中获取脚本文件，使其可执行，并将其移动到`/usr/local/bin`，使其成为沙箱用户的系统范围命令。
* **身份验证：** 脚本必须要求身份验证令牌作为其第一个参数。它应该将此令牌与存储在环境变量中的秘密值（例如`INSTALL_TOKEN`）进行比较。如果令牌无效，它必须退出并显示错误。
```

**5. 文档 (`README.md`)**

* 提供清晰简洁的README文件，解释架构和用法，面向管理员（如何部署和添加新命令）和分析师（有哪些命令可用）。
* **需求：** 整个`README.md`文件必须用**中文**编写。

**最终输出：**

请为以下所有文件提供完整的、充分注释的内容，结构清晰：

* `deploy.sh`
* `docker-compose.yml`
* `Dockerfile`
* `fetch_data.sh`
* `sensitive_data_scanner.py`
* `install_command.sh`
* `README.md`